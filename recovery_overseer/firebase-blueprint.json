{
  "collections": [
    {
      "name": "users",
      "description": "User account metadata and configurations",
      "rules": "Users can only read and write their own document where document ID matches their UID.",
      "schema": {
        "uid": "string (Firebase Auth UID)",
        "email": "string",
        "displayName": "string",
        "lastLogin": "timestamp",
        "createdAt": "timestamp",
        "theme": "string",
        "globalSettings": "map"
      }
    },
    {
      "name": "sessions",
      "description": "Session states saved across devices",
      "rules": "Users can only read, create, update, and delete documents where userId matches their UID.",
      "schema": {
        "userId": "string (Firebase Auth UID)",
        "title": "string",
        "lastActive": "timestamp",
        "activeTab": "string",
        "notebookState": "string (code)",
        "createdAt": "timestamp"
      }
    },
    {
      "name": "workflows",
      "description": "Cross-device workflow configurations",
      "rules": "Users can only read, create, update, and delete documents where userId matches their UID.",
      "schema": {
        "userId": "string (Firebase Auth UID)",
        "name": "string",
        "config": "map",
        "createdAt": "timestamp",
        "updatedAt": "timestamp"
      }
    }
  ]
}
